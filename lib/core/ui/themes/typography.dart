import 'package:flutter/material.dart';

/// Placeholder for future custom typography. Material 3's default
/// [TextTheme] (derived from the color scheme) is used until a brand
/// typeface is introduced.
abstract final class AppTypography {
  static TextTheme textTheme(TextTheme base) => base;
}
